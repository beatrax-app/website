---
import Base from "../layouts/Base.astro";
import DocPage from "../components/DocPage.astro";
import { docFor } from "../i18n/pages";

const doc = docFor("en", "audience")!;
---

<Base locale="en" active="audience" title={doc.title} description={doc.lead}>
  <DocPage doc={doc} />
</Base>
