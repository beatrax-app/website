---
import Base from "../layouts/Base.astro";
import Roadmap from "../components/Roadmap.astro";
import { copyFor } from "../i18n";

const t = copyFor("en");
---

<Base locale="en" active="roadmap" title={t.roadmap.title} description={t.roadmap.lead}>
  <Roadmap locale="en" />
</Base>
